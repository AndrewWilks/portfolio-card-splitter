# Task 0.5: Update Entities

**Phase**: 0 - Cleanup  
**Status**: ⬜ Not Started  
**Estimated Time**: 3-4 hours

## Problem

Entity interfaces and Zod schemas must match updated database schemas.

## Todo

### Payment Entity

- [ ] Update `shared/entities/payment.ts` interface
- [ ] Make `potId` required (not optional)
- [ ] Make `transactionId` required
- [ ] Make `amountCents` required
- [ ] Add `paidOn: Date` field (required)
- [ ] Keep `reservationId` optional
- [ ] Rename `description` to `note` (optional) if database renamed
- [ ] Update Zod schema to enforce required fields
- [ ] Update factory method
- [ ] Update toJSON method

### Reservation Entity

- [ ] Update `shared/entities/reservation.ts` interface
- [ ] Add `allocationId?: string` field (optional initially)
- [ ] Add `memberId: string` field (required or optional based on migration)
- [ ] Update Zod schema
- [ ] Update factory method
- [ ] Add getter methods for new fields

### Allocation Entity

- [ ] Update `shared/entities/allocation.ts`
- [ ] Remove `calculatedAmountCents` from persisted data
- [ ] Verify calculation methods exist (already done)
- [ ] Update toJSON to not include calculated field
- [ ] Update Zod schema if needed

### Transfer Entity

- [ ] Verify `shared/entities/transfer.ts` handles nullable pot IDs
- [ ] Already done in previous session, just verify

## Files to Modify

- `shared/entities/payment.ts`
- `shared/entities/reservation.ts`
- `shared/entities/allocation.ts`
- `shared/entities/transfer.ts` (verify only)

## Acceptance Criteria

### Payment

- [ ] Interface matches database schema
- [ ] potId, transactionId, amountCents, paidOn are required
- [ ] reservationId, note are optional
- [ ] Zod schema validates required fields
- [ ] Factory method enforces required fields
- [ ] Cannot create payment without required fields

### Reservation

- [ ] Interface includes allocationId and memberId
- [ ] Zod schema validates new fields
- [ ] Factory method accepts new fields
- [ ] Can create reservation with allocation link

### Allocation

- [ ] calculatedAmountCents not in persisted data
- [ ] Calculation methods work correctly
- [ ] toJSON doesn't include calculated field

### Transfer

- [ ] Supports nullable pot IDs
- [ ] Has isCashIn, isCashOut, isPotTransfer methods
- [ ] Validates at least one pot ID is present

## Verification

```bash
# Run all entity tests
deno test shared/__tests__/entities/payment.test.ts
deno test shared/__tests__/entities/reservation.test.ts
deno test shared/__tests__/entities/allocation.test.ts
deno test shared/__tests__/entities/transfer.test.ts

# Test creation with new fields
deno test -A --filter "create" shared/__tests__/entities/

# Test validation
deno test -A --filter "validation" shared/__tests__/entities/
```

## Example Updates

### Payment Entity

```typescript
export interface PaymentData extends EntityData {
  potId: string; // ✅ Required (was optional)
  transactionId: string; // ✅ Required
  amountCents: Cents; // ✅ Required
  paidOn: Date; // ✅ Added
  reservationId?: string; // Optional
  note?: string; // Optional (renamed from description?)
}

export const paymentSchema = z.object({
  potId: z.string().uuid(), // Required
  transactionId: z.string().uuid(), // Required
  amountCents: z.number().int().positive(), // Required
  paidOn: z.date(), // Required
  reservationId: z.string().uuid().optional(),
  note: z.string().optional(),
});
```

### Reservation Entity

```typescript
export interface ReservationData extends EntityData {
  potId: string;
  transactionId: string;
  allocationId?: string; // ✅ Added
  memberId: string; // ✅ Added (or optional initially)
  amountCents: Cents;
}

export const reservationSchema = z.object({
  potId: z.string().uuid(),
  transactionId: z.string().uuid(),
  allocationId: z.string().uuid().optional(), // Added
  memberId: z.string().uuid(), // Added
  amountCents: z.number().int().positive(),
});
```

## Notes

- Update tests as you go to ensure validation works
- Breaking change for Payment - all payment creation must include paidOn
- Reservation changes affect reservation creation flow
- Coordinate with Task 0.2 and 0.4 (database schemas)

## Dependencies

- Task 0.1, 0.2, 0.3, 0.4 (all database schema updates) must be complete

## Next Task

0.6-update-repositories.md
