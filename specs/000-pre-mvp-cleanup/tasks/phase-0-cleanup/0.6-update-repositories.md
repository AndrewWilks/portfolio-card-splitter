# Task 0.6: Update Repositories

**Phase**: 0 - Cleanup  
**Status**: ⬜ Not Started  
**Estimated Time**: 2-3 hours

## Problem

Repository queries must handle updated database schemas and entity interfaces.

## Todo

### Payment Repository

- [ ] Update `backend/repositories/paymentRepository.ts`
- [ ] Add `paidOn` field to queries
- [ ] Ensure `potId` is always provided (NOT NULL)
- [ ] Update create method signature
- [ ] Update update method if exists
- [ ] Update list/find queries to include paidOn
- [ ] Handle note vs description field rename

### Reservation Repository

- [ ] Update `backend/repositories/reservationRepository.ts`
- [ ] Add `allocationId` to queries
- [ ] Add `memberId` to queries
- [ ] Update create method signature
- [ ] Add query methods: findByMember, findByAllocation
- [ ] Update list queries to include new fields

### Allocation Repository

- [ ] Update `backend/repositories/allocationRepository.ts`
- [ ] Remove `calculatedAmountCents` from SELECT queries
- [ ] Remove from INSERT queries
- [ ] Remove from UPDATE queries
- [ ] Verify queries only fetch percentage/amountCents

### Transfer Repository

- [ ] Verify `backend/repositories/transferRepository.ts`
- [ ] Ensure queries handle nullable pot IDs
- [ ] Add filters for cash transactions (fromPotId IS NULL or toPotId IS NULL)

## Files to Modify

- `backend/repositories/paymentRepository.ts`
- `backend/repositories/reservationRepository.ts`
- `backend/repositories/allocationRepository.ts`
- `backend/repositories/transferRepository.ts`

## Acceptance Criteria

### Payment Repository

- [ ] All queries include paidOn field
- [ ] Create method requires potId, transactionId, amountCents, paidOn
- [ ] Cannot create payment without required fields
- [ ] List queries return paidOn
- [ ] All payment repository tests pass

### Reservation Repository

- [ ] All queries include allocationId, memberId
- [ ] Create method requires new fields
- [ ] Can query by member: `findByMemberId(memberId)`
- [ ] Can query by allocation: `findByAllocationId(allocationId)`
- [ ] All reservation repository tests pass

### Allocation Repository

- [ ] No queries reference calculatedAmountCents
- [ ] Queries only fetch stored fields
- [ ] All allocation repository tests pass

### Transfer Repository

- [ ] Handles nullable pot IDs correctly
- [ ] Can filter for cash transactions
- [ ] All transfer repository tests pass

## Verification

```bash
# Run repository tests
deno test backend/__tests__/repositories/paymentRepository.test.ts
deno test backend/__tests__/repositories/reservationRepository.test.ts
deno test backend/__tests__/repositories/allocationRepository.test.ts
deno test backend/__tests__/repositories/transferRepository.test.ts

# Run all repository tests together
deno test backend/__tests__/repositories/
```

## Example Updates

### Payment Repository

```typescript
async create(data: {
  potId: string;           // Required
  transactionId: string;   // Required
  amountCents: number;     // Required
  paidOn: Date;            // Required (added)
  reservationId?: string;
  note?: string;
}): Promise<Payment> {
  const result = await this.db
    .insert(payments)
    .values({
      potId: data.potId,
      transactionId: data.transactionId,
      amountCents: data.amountCents,
      paidOn: data.paidOn,  // Include new field
      reservationId: data.reservationId,
      note: data.note,
      createdById: data.createdById,
    })
    .returning();

  return Payment.create(result[0]);
}
```

### Reservation Repository

```typescript
async create(data: {
  potId: string;
  transactionId: string;
  allocationId?: string;  // Added
  memberId: string;       // Added
  amountCents: number;
}): Promise<Reservation> {
  const result = await this.db
    .insert(reservations)
    .values({
      potId: data.potId,
      transactionId: data.transactionId,
      allocationId: data.allocationId,  // Include
      memberId: data.memberId,          // Include
      amountCents: data.amountCents,
      createdById: data.createdById,
    })
    .returning();

  return Reservation.create(result[0]);
}

async findByMemberId(memberId: string): Promise<Reservation[]> {
  const results = await this.db
    .select()
    .from(reservations)
    .where(eq(reservations.memberId, memberId));

  return results.map(r => Reservation.create(r));
}
```

## Notes

- Update tests as you update repository methods
- Breaking changes require test updates
- Coordinate with entity updates (Task 0.5)

## Dependencies

- Task 0.5 (Entity updates) must be complete
- Database migrations (Tasks 0.1-0.4) must be applied

## Next Task

0.7-update-services-routes.md
