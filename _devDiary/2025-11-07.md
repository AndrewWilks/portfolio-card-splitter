# Dev Diary Entry - 2025-11-07

## Entity Audit & Pre-MVP Cleanup Spec

Conducted comprehensive audit of existing shared entities against product specifications to identify implementation gaps.

### What We Did

#### **1. Audited All Shared Entities**

Systematically reviewed each entity file in `/shared/entities/` to compare implementation against requirements in:

- `/specs/0. high level.md` (product spec)
- `/specs/1. architecture.md` (technical spec)

Found the shared layer has:

- ✅ 15 entity files implemented
- ❌ 2 critical entities missing (CardAccount, Card)
- ⚠️ 4 entities need refinement (Transaction, Reservation, Payment, Pot)

#### **2. Discovered Critical Gaps**

Used `grep_search` for "CardAccount|Card" across `/shared/entities/*.ts` → **no matches found**

This confirmed the biggest gap: core entities referenced throughout the spec don't exist yet.

**Missing entities**:

- **CardAccount**: Represents credit card accounts (issuer, name, last4, billing cycle, credit limit) - CRITICAL blocker
- **Card**: Optional physical/virtual card attribution within an account - nice-to-have

**Entity refinements needed**:

- **Transaction**: Missing `cardAccountId` (required) and `cardId` (optional) fields
- **Reservation**: Missing `allocationId` and `memberId` to link reservations to specific member allocations
- **Payment**: All fields currently optional (too permissive), should require potId, transactionId, amountCents, paidOn
- **Pot**: Uses `ownerId` (inconsistent with spec language "User")

#### **3. Created Pre-MVP Cleanup Spec**

Created `/specs/000-pre-mvp-cleanup/spec.md` documenting:

- **Missing entities** (CardAccount, Card) with proposed interfaces
- **Entity refinements required** (Transaction, Reservation, Payment, Pot) with specific field additions
- **Validation gaps** organized by entity (Allocation ✅ done, Transaction ⚠️ partial, Payment ❌ missing, Reservation ⚠️ partial, Pot ✅ good)
- **Implementation order** (Phase 1: CardAccount/Card entities, Phase 2: validation, Phase 3: polish)
- **Spec alignment checklist** (CardAccount created, Transaction updated, Payment required fields, Reservation linked to Allocation, exports updated, architecture doc updated, tests added)

### Key Outcomes

- Gap analysis complete: **2 missing entities, 4 need refinement**
- CardAccount identified as highest priority (transactions cannot be properly modeled without it)
- Pre-MVP cleanup spec provides clear roadmap for bringing entity model into full alignment
- Next steps: implement CardAccount entity, then update Transaction to reference it

### Technical Details

**CardAccount interface (proposed)**:

```typescript
interface CardAccountData extends EntityData {
  name: string; // User-friendly name
  issuer: string; // e.g., "Chase", "Amex"
  last4: string; // Last 4 digits
  billingCycle: number; // Day of month (1-31)
  creditLimitCents?: Cents; // Optional credit limit
  ownerId: string; // User who owns this account
  isActive: boolean; // Soft delete support
}
```

**Business rules**:

- Cannot delete if transactions exist (archive with `isActive: false`)
- If only one CardAccount exists, frontend auto-selects for new transactions
- Outstanding balance calculated per CardAccount: `sum(transactions) - sum(payments)`

**Card interface (proposed)**:

```typescript
interface CardData extends EntityData {
  cardAccountId: string; // Parent CardAccount
  memberId?: string; // Who typically uses this card
  nickname?: string; // e.g., "Alice's card", "Virtual #1"
  last4?: string; // Last 4 of card number
  isActive: boolean; // Soft delete support
}
```

**Business rules**:

- Cards are optional (transactions can exist without Card attribution)
- Cards don't affect splits or payments (attribution only for filtering/reporting)

### Reflection

The entity audit revealed the spec work from yesterday was critical - without clarifying CardAccount vs Card distinction, we would have continued building on flawed assumptions. The systematic grep search approach proved effective for confirming suspicions about missing entities. The gap analysis document will serve as a clear implementation roadmap.

**Next session priorities**:

1. Implement CardAccount entity with factory, validation, and tests
2. Implement Card entity with factory, validation, and tests
3. Update Transaction entity to add cardAccountId (required) and cardId (optional)
4. Update shared/entities/index.ts exports
5. Update architecture.md domain model section

---

## Implementation Plan Creation

After completing the gap analysis, created comprehensive implementation plan addressing user's concern about cleaning up outdated code first.

### What We Did

#### **1. Codebase Audit for Cleanup Items**

Systematically reviewed backend database schema, entities, repositories, and services to identify code that conflicts with updated specs:

**Found misalignments**:

- **Transfer table**: Both `fromPotId` and `toPotId` marked NOT NULL (spec says nullable for cash transactions)
- **Payment table**: `potId` is nullable (spec says required), missing `paidOn` field
- **Allocation table**: Persists `calculatedAmountCents` (violates derived value pattern)
- **Reservation table**: Missing `allocationId` and `memberId` links to track member-specific reservations

**Confirmed no shared/repositories** - Architecture correctly separates backend repos (talk to database) from frontend repos (talk to API)

#### **2. Created Three-Phase Implementation Plan**

Created `/specs/000-pre-mvp-cleanup/implementation-plan.md` with comprehensive roadmap:

**Phase 0: Pre-Implementation Cleanup** 🧹

- Database schema fixes (transfers, payments, allocations, reservations)
- Entity schema updates to match corrected database
- Repository updates to handle new constraints
- Service updates to enforce business rules
- Route/API updates for new validation
- Test updates to reflect schema changes
- Migration scripts with rollback procedures

**Phase 1: Critical Missing Entities** 🚨

- Create CardAccount entity (name, issuer, last4, billingCycle, creditLimit, owner, isActive)
- Create Card entity (cardAccountId, memberId, nickname, last4, isActive)
- Update Transaction entity to add cardAccountId (required) and cardId (optional)
- Database tables and migrations
- Repositories, services, routes for new entities
- Comprehensive test coverage
- Update entity exports

**Phase 2: Validation & Business Rules** ✅

- Payment validation (cannot exceed transaction total, flags mismatches)
- Reservation validation (links to allocation, respects pot balance)
- Transaction validation (allocations sum to total, card belongs to account)
- Allocation validation (one-of rule, totals reconcile)
- Pot validation (scope-based visibility)

**Phase 3: Polish & Documentation** 📝

- Service layer refinements (ledger calculations per CardAccount)
- Entity relationship documentation
- Architecture doc updates (add CardAccount/Card to domain model)
- Gap analysis checklist updates

#### **3. Defined Clear Success Criteria**

For each phase, documented:

- Completion checklist (what done looks like)
- Test coverage goals (entity 100%, repo 90%, service 90%, route 85%)
- Migration strategy with rollback procedures
- Risk mitigation (data migration, breaking changes)

#### **4. Addressed Data Migration Concerns**

**Key decision**: Create default "Legacy Account" CardAccount for existing transactions

```sql
-- Smooth migration path
INSERT INTO card_accounts (name, issuer, last4, billing_cycle, owner_id, is_active)
VALUES ('Legacy Account', 'Unknown', '0000', 1, (SELECT id FROM users LIMIT 1), true);

UPDATE transactions
SET card_account_id = '<default_card_account_id>'
WHERE card_account_id IS NULL;
```

This prevents blocking existing transaction data while enforcing the new requirement going forward.

### Key Outcomes

- **Zero-ambiguity roadmap**: Every file to create/update documented
- **Phase 0 identified**: 7 cleanup tasks that must happen BEFORE adding new features
- **Database migrations**: All schema changes documented with rollback procedures
- **Test strategy**: Clear coverage goals and validation checkpoints
- **Timeline**: 9-13 days estimated for complete implementation
- **Risk mitigation**: Data migration strategy for existing transactions

### Technical Details

**Phase 0 Schema Fixes**:

1. Transfers: Make pot IDs nullable + add check constraint
2. Payments: Make potId NOT NULL, add paidOn field
3. Allocations: Drop calculatedAmountCents column
4. Reservations: Add allocationId and memberId columns

**Phase 1 New Tables**:

- `card_accounts` (7 fields + timestamps)
- `cards` (5 fields + timestamps)
- Update `transactions` to add cardAccountId and cardId

**Critical Path**: Phase 0 must complete before Phase 1 (can't add CardAccount entities until payment/transfer schemas are fixed)

### Reflection

The implementation plan addresses the user's key insight: **clean up what's broken before adding what's missing**. Phase 0 ensures we're building on a solid foundation aligned with the current specs. The phased approach with clear checkpoints (all tests pass between phases) reduces risk of compounding issues.

The data migration strategy (default "Legacy Account") is critical - it allows smooth migration of existing transactions without manual intervention while enforcing the new CardAccount requirement for all future transactions.

**Implementation ready to begin**: All gaps documented, all files identified, all migrations specified. Just need to create feature branch and start Phase 0.
