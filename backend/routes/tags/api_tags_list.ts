import { Context } from "hono";

export function apiTagsList(c: Context) {
  // TODO: Implement GET /api/tags endpoint to list tags
  // - Return tags array
  return c.json({ message: "Not implemented" }, 501);
}
